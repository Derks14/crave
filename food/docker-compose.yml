services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      MONGODB_INITDB_ROOT_USERNAME: root
      MONGODB_INTIDB_ROOT_PASSWORD: password
      MONGODB_INITDB_DATABASE: product-service
    volumes:
      - ./data:/data/db